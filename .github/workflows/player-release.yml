name: Build Player Release

on:
  workflow_dispatch:
    inputs:
      tag:
        description: 'Release tag (e.g. v0.12.0-player)'
        required: true
        type: string

env:
  CARGO_TERM_COLOR: always

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-msvc
      - name: Build player
        run: cargo build --release --features player --target x86_64-pc-windows-msvc
      - name: Package
        run: |
          mkdir bundle
          copy target\x86_64-pc-windows-msvc\release\protocol-play.exe bundle\protocol-player-windows.exe
          copy levels\paula.json bundle\level.json
        shell: cmd
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: windows-bundle
          path: bundle/

  build-macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-apple-darwin
      - name: Build player
        run: cargo build --release --features player --target aarch64-apple-darwin
      - name: Create and zip .app bundle
        run: |
          mkdir -p "Protocol Play.app/Contents/MacOS"
          mkdir -p "Protocol Play.app/Contents/Resources"
          cp target/aarch64-apple-darwin/release/protocol-play "Protocol Play.app/Contents/MacOS/protocol-play"
          chmod +x "Protocol Play.app/Contents/MacOS/protocol-play"
          cp levels/paula.json "Protocol Play.app/Contents/MacOS/level.json"
          cp macos/Info.plist "Protocol Play.app/Contents/Info.plist"
          ditto -c -k --sequesterRsrc --keepParent "Protocol Play.app" protocol-player-macos.zip
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: macos-zip
          path: protocol-player-macos.zip

  release:
    needs: [build-windows, build-macos]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Prepare release files
        run: |
          cp artifacts/macos-zip/protocol-player-macos.zip .
          cd artifacts/windows-bundle && zip ../../protocol-player-windows.zip protocol-player-windows.exe level.json

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ inputs.tag }}
          name: "Player ${{ inputs.tag }}"
          body: |
            Player build for testing levels.

            **Windows**: Extract the zip and run `protocol-player-windows.exe`. Level included.
            **macOS**: Extract the zip, run `xattr -cr "Protocol Play.app"`, then double-click. Level included.
          files: |
            protocol-player-windows.zip
            protocol-player-macos.zip
